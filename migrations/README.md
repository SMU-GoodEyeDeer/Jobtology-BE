# Application migrations

BE owns migrations for `jobtology_app` only. No tables are created by the scaffold.
Initialize Alembic with SQLAlchemy models during milestone 2. Pipeline migrations remain in
Jobtology-DB. Database creation and roles must be coordinated with its deployment bootstrap.
